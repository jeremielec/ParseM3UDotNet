using System;
using ParseM3UNet.Http;

namespace ParseM3UNet.Helpers;

public class BlockUtils
{

    // public static long GetBlockForOffset(long offset) => (long)double.Floor(offset / HttpDownloaderBlockClient.BlockSize);
    // public static long GetOffsetForBlock(long block) => block * HttpDownloaderBlockClient.BlockSize;

}
